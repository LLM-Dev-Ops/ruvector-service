# =============================================================================
# RuvVector Service - Environment Configuration
# SPARC Specification Compliant
# =============================================================================

# -----------------------------------------------------------------------------
# Required Environment Variables
# -----------------------------------------------------------------------------

# Service listen port
PORT=3000

# RuvVector connection (infra-provisioned)
RUVVECTOR_HOST=ruvvector.infra.svc.cluster.local
RUVVECTOR_PORT=6379

# Logging verbosity: debug, info, warn, error
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Optional Environment Variables (with defaults)
# -----------------------------------------------------------------------------

# RuvVector tuning
RUVVECTOR_TIMEOUT=30000       # Request timeout (ms), default: 30000
RUVVECTOR_POOL_SIZE=10        # Connection pool size, default: 10

# Circuit breaker configuration
CIRCUIT_BREAKER_THRESHOLD=5   # Failures before opening, default: 5
CIRCUIT_BREAKER_TIMEOUT=30000 # Open state duration (ms), default: 30000
CIRCUIT_BREAKER_RESET=60000   # Time before full reset (ms), default: 60000

# Metrics configuration
METRICS_ENABLED=true          # Enable Prometheus metrics, default: true
METRICS_PORT=9090             # Metrics endpoint port, default: 9090

# Graceful shutdown
SHUTDOWN_TIMEOUT=30000        # Graceful shutdown timeout (ms), default: 30000

# =============================================================================
# Variable Sources (SPARC Requirement)
# =============================================================================
# All variables MUST come from:
# 1. Environment variables set by container orchestrator
# 2. ConfigMaps/Secrets mounted as environment variables
# 3. Service mesh sidecar injection
#
# Variables MUST NOT come from:
# 1. Hardcoded values in code
# 2. Configuration files in repository
# 3. External configuration services
# 4. Command-line arguments
# =============================================================================
